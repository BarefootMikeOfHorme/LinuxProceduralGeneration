# Archive Directory

This directory contains files that were moved during project organization cleanup (2025-12-09).

## Structure

### `temp_files/`
Contains temporary files, test outputs, and development artifacts that were cluttering the root directory:

- **Test directories**: cli_real_test/, cli_test/, test_cli_output/, test_unicode_fix/, test_updated/, test_final/, Testing/
- **Test images**: test_*.png, smoke_test_*.png
- **Build artifacts**: build.log, build_debug.txt, build_debug_venv.txt
- **Temp text files**: folder_structure_temp.txt, structure.txt, ascii_art.txt, TEST_RESULTS.txt
- **IDE/Tool files**: "FOR CURSOR .txt", "FOR CURSOR .zip", "#  NEW BROWSER-LIKE HEADERS FOR REQ.txt"
- **Archives**: vaultmind_forge.tar.zst
- **Misc test outputs**: beach_girl/, nul

## Why These Were Archived

These files were development artifacts that:
- Should not be in version control (build outputs, temp files)
- Were from previous development/testing sessions
- Cluttered the root directory and made the project look unprofessional
- May contain useful information but are not needed for daily development

## When to Delete

After verifying the project builds and runs correctly without these files (1-2 weeks), this entire archive directory can be safely deleted.

## Restoration

If you need any of these files, they are preserved here with their original directory structure.
